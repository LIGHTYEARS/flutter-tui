# Phase 10: Tool Compatibility — Plan 01

**Wave:** 1 of 1
**Scope:** TOOL-01 through TOOL-04

## Tasks

### Task 1: Tool name normalization map (TOOL-01)

**File:** `packages/flitter-amp/src/widgets/tool-call/tool-call-widget.ts`

Add a normalization map that maps common agent tool variants to the internal canonical names used by the switch statement. Apply normalization before the switch:

```
read_file, ReadFile → Read
execute_command, shell, run_command, terminal → Bash
search, grep, ripgrep, find_files, list_files → Grep
write_file, write_to_file, WriteFile → create_file
edit, str_replace_editor, EditTool → edit_file
web_search, browser, fetch_url → WebSearch
```

### Task 2: Fix ToolCallResult.content extraction (TOOL-02)

**Files:** All tool files with `extractOutput()` methods

Fix content extraction to handle both flat `{type, text}` and nested `{type, content: {type, text}}` structures:
- Change `c.content?.text ?? ''` to `c.text ?? c.content?.text ?? ''`

Affected files:
- `bash-tool.ts` line 120
- `read-tool.ts` line 101
- `grep-tool.ts` line 110
- `web-search-tool.ts` line 126
- `generic-tool-card.ts` lines 178, 199

### Task 3: Use kind/title in ToolHeader name (TOOL-03)

**Files:** `read-tool.ts`, `create-file-tool.ts`

Two specialized tools have hardcoded ToolHeader names:
- `read-tool.ts:52` hardcodes `name: 'Read'` — change to `name: this.toolCall.kind ?? 'Read'`
- `create-file-tool.ts:41` hardcodes `name: 'CreateFile'` — change to `name: this.toolCall.kind ?? 'CreateFile'`

(bash-tool, grep-tool, web-search-tool, generic-tool-card already use `this.toolCall.kind`)

### Task 4: Resilient rawInput field extraction (TOOL-04)

**Files:** `bash-tool.ts`, `read-tool.ts`, `grep-tool.ts`, `create-file-tool.ts`, `web-search-tool.ts`

Add additional field name variants:
- **BashTool**: add `shell_command`, `script`, `args` (currently: `command`, `cmd`)
- **ReadTool**: add `filename`, `file` (currently: `file_path`, `path`)
- **GrepTool**: add `search_pattern`, `regex`, `search`, `directory` for path (currently: `pattern`, `query`, `glob`, `path`)
- **CreateFileTool**: add `filename`, `file`, `destination` (currently: `file_path`, `path`)
- **WebSearchTool**: add `search_query`, `q`, `search` (currently: `query`, `url`)

---

*Plan: 10-PLAN-01*
*Created: 2026-03-28*
